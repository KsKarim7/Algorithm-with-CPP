#include <bits/stdc++.h>
using namespace std;

const int N = 1e3 + 5;
bool vis[N][N];

int main()
{
    int n, m;
    cin >> n >> m;
    char arr[n][m];
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < m; j++)
        {
            cin >> arr[i][j];
        }
    }
    int si, sj;
    cin >> si >> sj;
    return 0;
}